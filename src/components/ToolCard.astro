---
import AffiliateLink from './AffiliateLink.astro';

interface Props {
  name: string;
  tagline: string;
  link: string;
}

const { name, tagline, link } = Astro.props;
---

<div class="border border-gray-200 rounded-lg p-6 hover:shadow-lg transition-shadow bg-white">
  <h3 class="text-xl font-semibold mb-2">{name}</h3>
  <p class="text-gray-600 mb-4">{tagline}</p>
  <AffiliateLink href={link} label="Learn More →" class="font-semibold" />
</div>
